<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jetnews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jetnews">
        <activity
            android:name=".CoolActivity"
            android:label="@string/title_activity_cool"
            android:theme="@style/Theme.Jetnews.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"></activity> <!-- Needed for UI tests -->
        <activity
            android:name="android.app.Activity"
            android:theme="@style/Theme.Jetnews" />
    </application>

</manifest>